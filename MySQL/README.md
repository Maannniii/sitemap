MySQL table file to dreate table for storing data.

### Running
* Create the table in mysql either by running `mysql -u user -p databasename < table.sql` or by copy pasting the content of table.sql into MySQL console.

